---
# tasks file for install-hashicorp-repo
- name: add Hashicorp repo
  get_url:
    url: '{{ hashicorp_mirror }}/{{ ansible_distribution_major_version }}/hashicorp.repo'
    dest: /etc/yum.repos.d/hashicorp.repo

    